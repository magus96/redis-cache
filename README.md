Distributed cache service using redis wrritten in golang. 

Run redis on port 5432 in Docker. Internal port for redis in Docker is 6379, map the port to 5432 externally. 

"make run" in the project directory to start service.
